/**
 * Custom exceptions for ArmorIQ SDK.
 */
/**
 * Base exception for all ArmorIQ SDK errors.
 */
export declare class ArmorIQException extends Error {
    constructor(message: string);
}
/**
 * Raised when an intent token is invalid.
 *
 * This can occur due to:
 * - Invalid signature
 * - Token expiration
 * - Token revocation
 * - Malformed token structure
 */
export declare class InvalidTokenException extends ArmorIQException {
    tokenId?: string;
    constructor(message: string, tokenId?: string);
}
/**
 * Raised when an action doesn't match the original intent plan.
 *
 * This occurs when trying to execute an action that was not
 * included in the canonicalized plan used to generate the token.
 */
export declare class IntentMismatchException extends ArmorIQException {
    action?: string;
    planHash?: string;
    constructor(message: string, action?: string, planHash?: string);
}
/**
 * Raised when an intent token has expired.
 *
 * Tokens have a validity period set during issuance.
 * This exception is raised when attempting to use an expired token.
 */
export declare class TokenExpiredException extends InvalidTokenException {
    expiredAt?: number;
    constructor(message: string, tokenId?: string, expiredAt?: number);
}
/**
 * Raised when an MCP action invocation fails.
 *
 * This can occur due to:
 * - MCP server unavailable
 * - Action not found
 * - Invalid parameters
 * - Proxy verification failure
 */
export declare class MCPInvocationException extends ArmorIQException {
    mcp?: string;
    action?: string;
    statusCode?: number;
    constructor(message: string, mcp?: string, action?: string, statusCode?: number);
}
/**
 * Raised when agent delegation fails.
 *
 * This can occur due to:
 * - Target agent unavailable
 * - Trust delegation rejected
 * - Invalid subtask structure
 */
export declare class DelegationException extends ArmorIQException {
    targetAgent?: string;
    delegationId?: string;
    statusCode?: number;
    constructor(message: string, targetAgent?: string, delegationId?: string, statusCode?: number);
}
/**
 * Raised when a policy enforcement blocks the action.
 */
export declare class PolicyBlockedException extends ArmorIQException {
    enforcementAction?: string;
    reason?: string;
    metadata?: Record<string, any>;
    constructor(message: string, enforcementAction?: string, reason?: string, metadata?: Record<string, any>);
}
/**
 * Raised when a policy enforcement holds the action for approval.
 */
export declare class PolicyHoldException extends ArmorIQException {
    delegationContext?: Record<string, any>;
    metadata?: Record<string, any>;
    constructor(message: string, delegationContext?: Record<string, any>, metadata?: Record<string, any>);
}
/**
 * Raised when SDK configuration is invalid.
 *
 * This can occur due to:
 * - Missing required configuration
 * - Invalid endpoint URLs
 * - Missing credentials
 */
export declare class ConfigurationException extends ArmorIQException {
    constructor(message: string);
}
//# sourceMappingURL=exceptions.d.ts.map