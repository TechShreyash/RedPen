# RedPen Frontend — Environment Variables
# Copy this file to .env.local for local development overrides.
# .env.local is gitignored and never committed.

# Override the backend API URL (defaults to deployed production backend)
# Uncomment the line below to use your local backend during development:
# VITE_API_BASE=http://localhost:8000/api
